# ทุก path ส่งกลับไปที่ index.html ให้ React Router จัดการเอง
#
# ระบบเปิดได้ทุกหน้าจาก URL ตรง (FND-009) เช่น /debts/D-0001 หรือ
# /receiving/Q-0001/receipt — ถ้าไม่มีบรรทัดนี้ Cloudflare จะหาไฟล์ชื่อนั้นไม่เจอ
# แล้วตอบ 404 ทันทีที่ผู้ใช้กด refresh หรือเปิดลิงก์ที่แชร์กันมา
/*    /index.html   200
